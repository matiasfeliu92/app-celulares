export default interface ICellphones {
    id?: number | null,
    name: string,
    price: number,
    brand_id_id: number | null,
    description: object,
    url_web: string | null,
    url_image: string | null,
  }

export default interface IProducts {
  id?: number | null,
  name: string,
  price: number,
  brand_id_id: number | null,
  description: object,
  url_web: string | null,
  url_image: string | null,
}